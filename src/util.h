#ifndef UTIL_HEADER
#define UTIL_HEADER

#include <stdint.h>
#include <cstdlib>
#include <cstring>
#include <vector>
#include <list>
#include <iostream>
#include <iomanip>
#include <unordered_map>

#define MIN(a, b) (((a) < (b)) ? (a) : (b))
#define MAX(a, b) (((a) > (b)) ? (a) : (b))

#endif
